#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/qemu-kvm" ] || [ -e "/etc/init/qemu-kvm.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d qemu-kvm stop || true
fi
# End automatically added section
